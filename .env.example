# Copia a server/.env para desarrollo local
# cp server/.env.example server/.env

# Variables para Vercel (Project Settings → Environment Variables):
#
# DATABASE_URL       → libsql://tu-db.turso.io  (para prisma migrate deploy)
# TURSO_DATABASE_URL → libsql://tu-db.turso.io  (runtime de la API)
# TURSO_AUTH_TOKEN   → token de Turso
